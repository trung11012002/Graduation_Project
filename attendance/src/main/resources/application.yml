#eureka:
#  instance:
#    hostname: localhost
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka

server:
  port: 8000
spring:
  application:
    name: attendance
  config:
    import: optional:configserver:http://localhost:8888
